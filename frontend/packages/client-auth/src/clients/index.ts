export {
	client as uniformClient,
	client,
	axiosInstance,
	getConfig,
	setConfig,
} from './uniform-client'
export type {
	RequestConfig,
	ResponseConfig,
	ResponseErrorConfig,
} from './uniform-client'

// Re-export the default export
export { default } from './uniform-client'
